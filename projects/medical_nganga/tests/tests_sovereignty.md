# Test de Validación: Soberanía Médica (Nganga)

1. **Test de Hardware**: Verificar que el fallback a CPU se activa sin errores de CUDA.
2. **Test de Métrica**: Confirmar que `dose_error_mae` se reporta correctamente en `results.tsv`.
3. **Test de AMV**: Validar que el agente `Architect` puede modificar el `train.py` local del proyecto.
